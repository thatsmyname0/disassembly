#ifndef _UTILS_H_
#define _UTILS_H_

#include<iostream>
#include<algorithm>
#include<cstring>
using std::string;

char getInsType(string ins);
int getInsFunc(string ins);
void assert(bool check);

#endif